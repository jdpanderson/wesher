version: "2"

linters:
  default: standard # errcheck, govet, ineffassign, staticcheck, unused
  enable:
    - misspell
    - unconvert
    - unparam
    - gocritic
  settings:
    govet:
      enable:
        - shadow

formatters:
  enable:
    - gofmt
